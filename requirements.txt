# --- Core LangChain family: pinned to the 0.3.x line ---
# EnsembleRetriever lives at `langchain.retrievers` only on this line; LangChain
# 1.x moved it into a separate `langchain_classic` package.
langchain-core>=0.3.78,<0.4.0
langchain>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langchain-huggingface==0.3.1

langchain-text-splitters>=0.3.0
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0
chromadb>=0.5.0
rank-bm25>=0.2.2
psutil>=5.9.0

# --- LLM providers (Phase 3) ---
# All three pinned to the last release compatible with langchain-core<0.4.0.
# DO NOT `pip install langchain-groq` / `langchain-ollama` / `langchain-openai`
# unpinned later — their newest releases require langchain-core>=1.0 and will
# silently upgrade it, breaking langchain-huggingface (which needs <1.0) and
# breaking the `from langchain.retrievers import EnsembleRetriever` import in
# rag_chain.py. Only install the ONE provider you're actually using.
langchain-groq==0.3.8
langchain-ollama==0.3.10
langchain-openai==0.3.35

# --- UI (Phase 4) ---
gradio==5.9.1
pydantic==2.10.6